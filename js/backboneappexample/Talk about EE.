Talk about EE.

Not just using EE because it's awesome, its because we've built and awesome framework on top of it. Allowing for greater maintenance and extensibilty. future proofing :(    - Build highly complex single page apps, run split tests, easy to work with template selectors (minimal UI) with standard widgets -